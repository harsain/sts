export class CategoryData {
  year: number;
  pns: number;
  faceValues: number;

  constructor(year: number, pns: number, faceValues: number) {
    this.year = year;
    this.pns = pns;
    this.faceValues = faceValues;
  }
}
